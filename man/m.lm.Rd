% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m.lm.R, R/m.pcr.R, R/m.ridge.R
\name{m.lm}
\alias{m.lm}
\alias{m.pcr}
\alias{m.ridge}
\title{Linear regression for tidyfit}
\usage{
m.lm(x, y, ...)

m.pcr(x, y, ncomp = NULL, ...)

m.ridge(x, y, lambda = NULL, ...)
}
\arguments{
\item{x}{Input matrix or data.frame, of dimension \eqn{(N\times p)}{(N x p)}; each row is an observation vector.}

\item{y}{Response variable.}

\item{...}{Additional arguments passed to \code{lm}.}
}
\value{
A 'tibble'.

A 'tibble'.

A 'tibble'.
}
\description{
Fits a linear regression and returns the results as a tibble. The function can be used with \code{tidyfit}.

Fits a linear regression and returns the results as a tibble. The function can be used with \code{tidyfit}.

Fits a linear regression and returns the results as a tibble. The function can be used with \code{tidyfit}.
}
\details{





}
\examples{
x = matrix(rnorm(100 * 20), 100, 20)
y = rnorm(100)
fit = hfr(x, y, kappa = 0.5)
coef(fit)

x = matrix(rnorm(100 * 20), 100, 20)
y = rnorm(100)
fit = hfr(x, y, kappa = 0.5)
coef(fit)

x = matrix(rnorm(100 * 20), 100, 20)
y = rnorm(100)
fit = hfr(x, y, kappa = 0.5)
coef(fit)

}
\references{





}
\seealso{
\code{tidypredict} method

\code{tidypredict} method

\code{tidypredict} method
}
\author{
Johann Pfitzinger

Johann Pfitzinger

Johann Pfitzinger
}
